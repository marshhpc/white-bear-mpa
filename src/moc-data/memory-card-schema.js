const memoryCardSchema = {
   id: String,
   imagery: String,
   answer: String,
   useerId: String,
   createdAt: Number,
   nextAttemptAt: Number,
   totalSuccessfulAttempts: Number,
   level: Number,
};
